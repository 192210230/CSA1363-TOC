#include<stdio.h>
#include<string.h>
int main()
{
	char str[100];
	int i;
	i=strlen(str);
	printf("enter the string: ");
	scanf("%s",&str);
    if(str[0]=='a'&& str[i-1]=='a')
	printf("accepted");
	else
	printf("not accepted");
	return 0;
}
